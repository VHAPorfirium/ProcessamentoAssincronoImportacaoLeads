# Copie para .env e preencha com seus valores. O .env não é commitado.

# Banco de dados
DB_NAME=AsynchronousProcessingLeadImport
DB_USER=root
DB_PASSWORD=sua_senha_aqui

# AWS (não usar access_key/secret_key no código; usar IAM Role na AWS ou perfil local)
AWS_REGION=
AWS_S3_BUCKET_LEADS=
AWS_SQS_QUEUE_URL=
AWS_SNS_TOPIC_ARN=
